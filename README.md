# eb-optimization
Tunes parameters and decision policies (e.g., adjustment layers, thresholds) against EB objectives using historical data, producing reproducible parameter artifacts that can be applied deterministically via without optimization dependencies.
